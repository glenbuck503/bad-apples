import Input from './../src/js/input.js';

describe('Input', () => {
  let input;

  beforeEach(() => {
    input = new Input();
  });

  test('create input object', () => {
    expect(input).toEqual();
  })
})